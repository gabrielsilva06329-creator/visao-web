node_modules/
.DS_Store
*.log
.env
.vscode/

# Claude Code harness files (added by Claude — not project source)
skills/

# Personal business tracker — not part of the website
VISAO-tracker.xlsx
build_tracker.py

# One-time build scripts (changes already applied to source HTML)
add_subpage_bg.py
add_cursor_trail.py

# Source/uncompressed images — the optimized .jpg versions are what ship
images/background2.png
images/background2fix.png
